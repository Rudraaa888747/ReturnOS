package com.returnos.task;

/** Work types that exist only to execute return operations. */
public enum TaskType {
    INSPECT_RETURN,
    RESTOCK_ITEM,
    REFURBISH_ITEM,
    SUBMIT_VENDOR_CLAIM,
    SEND_TO_LIQUIDATION,
    PROCESS_RECYCLING,
    SCRAP_ITEM
}
