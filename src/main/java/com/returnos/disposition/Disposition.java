package com.returnos.disposition;

/** Possible outcomes for an inspected return. */
public enum Disposition {
    RESTOCK,
    REFURBISH,
    RESELL,
    RETURN_TO_VENDOR,
    LIQUIDATE,
    RECYCLE,
    SCRAP
}
